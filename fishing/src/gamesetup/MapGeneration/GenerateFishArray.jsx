// useGenerateFishArray.js
import { useCallback } from 'react';

// Custom Hook for generating random names
const useGenerateFishArray = () => {
  const generateFishArray = useCallback(() => {

  }, []);

  return generateFishArray;
};

export default useGenerateFishArray;
